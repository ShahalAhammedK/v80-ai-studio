fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
Pillow>=11.0
numpy>=2.0
openai>=1.59
google-genai>=1.0
python-dotenv>=1.0

# Live person segmentation. NOT needed in production: the built-in campaign
# images ship with precomputed masks (see backend/precomputed_masks.py), and
# these pull in ~320MB of deps plus ~176MB of weights and ~1GB of peak RAM.
# Install them only to regenerate a mask after changing a campaign image:
#   pip install -r requirements-dev.txt
